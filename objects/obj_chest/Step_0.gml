// ======================================================================
// obj_chest - Step 事件（V3 标准）
// ======================================================================

// ============================================================
// 1. ★ 已打开则不再处理交互 ★
// ============================================================
if (is_open) exit;


// ============================================================
// 2. ★ 辉光脉冲计时器 ★
// ============================================================
pulse_timer += 0.05 * glow_pulse_speed;


// ============================================================
// 3. 检测玩家是否在交互范围内
// ============================================================
var _player = instance_nearest(x, y, obj_player);
if (instance_exists(_player)) {
    var _dist = point_distance(x, y, _player.x, _player.y);
    player_nearby = (_dist < bbox_radius);
} else {
    player_nearby = false;
}


// ============================================================
// 4. ★ 按键提示呼吸 ★
// ============================================================
if (player_nearby) {
    interact_timer += 0.05;
}


// ============================================================
// 5. ★ 交互输入 ★
// ============================================================
if (player_nearby && keyboard_check_pressed(ord("E"))) {
    scr_chest_open(id);
    show_debug_message("🔑 宝箱被打开！");
}